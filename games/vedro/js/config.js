// ─── ВСЕ константы игры «ВЕДРО» ──────────────────────────────────────────────
// Мир фиксированной ширины, высота подгоняется под вьюпорт в main.js.
// Всё, что ниже, — в координатах мира, а не экрана.

export const CONF = {
  version: 'vedro-1.1.0',

  width: 960,
  height: 600,        // пересчитывается в main.js

  groundH: 110,       // толщина земли от низа мира
  cliffX: 720,        // край пропасти: правее — пустота

  // пиксель-арт: текстуры рисуются мелкими, увеличиваются целыми числами
  px: {
    bucket: 4,        // 90×100 → 360×400
    person: 3,        // 8×12   → 24×36
    llama: 3,
  },

  bucket: {
    texW: 90,
    texH: 100,
    botInset: 12,     // низ трапеции уже верха — отсюда настоящая точка опоры
    maxLean: 7,       // до опрокидывания ведро лишь кренится: наклонись оно
                      // сильнее — низ оторвётся от толпы и она будет толкать
                      // воздух. Всё напряжение — в дрожи, а не в угле
    fallAt: 104,      // за этим углом центр тяжести уже за точкой опоры
    soloWobble: 2.4,  // на что способен один человечек
    ease: 2.2,        // скорость подхода наклона к цели, 1/сек
    releaseEase: 1.4, // скорость отката, когда никто не толкает
    creakFrom: 1.2,   // с какого наклона начинает скрипеть
  },

  player: {
    speed: 200,       // px/сек
    startX: 120,
  },

  crowd: {
    needed: 1000,     // столько толкателей опрокидывают ведро
    // Толпа — клин, уходящий в глубину: ряды мельчают, светлеют от дымки
    // и сдвигаются вправо, за ведро. Подъём ряда пропорционален его масштабу,
    // иначе дальние головы слипаются в сплошную тёмную плиту
    rows: 18,
    slotGap: 8,       // плотнее, чем человечек: это давка, а не очередь
    leftEdge: 12,     // левее человечки уже за кадром
    leftShift: 5,     // дальние ряды не достают до края — отсюда перспектива
    rowShift: 7,      // насколько ряд вглубь заходит вправо, за ведро
    rowShiftMax: 240,
    rowLift: 8,       // подъём переднего ряда; дальние — меньше, по масштабу
    rowScale: 0.032,
    rowHaze: 0.72,    // насколько дальний ряд выцветает в небо
    joinAfter: 800,   // мс непрерывного толкания до первого помощника
    // Приход не по интервалу, а по скорости: она растёт от каждого пришедшего,
    // то есть dN/dt = base + accel·N — честная геометрическая прогрессия:
    // сперва подтягиваются по одному, а под конец экран заливает толпой
    rateBase: 2,      // человек/сек в самом начале
    rateAccel: 0.26,  // прибавка к скорости за каждого уже пришедшего
    leaveEvery: 380,  // если перестать толкать — расходятся
    runSpeed: 340,
    llamaWorth: 10,   // лама толкает за десятерых
    countMul: 1000,   // на табло числа эпические, а не настоящие
    partyAmp: 20,     // высота прыжка от радости
  },

  // Через столько секунд ведро устаёт стоять и возносится
  timeLimit: 40,

  // Сколько дать полюбоваться финалом, прежде чем накрыть экраном итогов.
  // Радующаяся толпа — лучшее, что есть в игре: не гасить её на полуслове
  hold: {
    topple: 4200,
    ascend: 6000,
  },

  hud: {
    throttleDesktop: 45,
    throttleTouch: 90,
  },

  storage: {
    muted: 'lamopad_vedro_muted',
    sfxMuted: 'lamopad_vedro_sfx_muted',
    fsDisabled: 'lamopad_vedro_fs_disabled',
    best: 'lamopad_vedro_best_people',
  },

  // подсказки микро-туториала: показываются по ходу дела, не простынёй
  hints: {
    move:  '← →  иди к ведру',
    push:  'ТОЛКАЙ!',
    solo:  'одному не под силу…',
    keep:  'не останавливайся!',
    horde: 'ещё! ещё человечков!',
    hurry: 'ВРЕМЯ!',
  },

  // То, что толпа поняла про ведро. Пишется прямо в игре над человечками
  // и повторяется на экране итогов — это одна и та же мысль
  loveLine: 'МЫ ПОЛЮБИЛИ ВЕДРО',
};

// Палитра — денди-ограниченная, ржаво-дорожная
export const PAL = {
  skyTop:    '#2a4a9c',
  skyMid:    '#5b8fe0',
  skyLow:    '#a8cdf0',
  sun:       '#ffd93f',
  cloud:     '#fdf6e8',

  groundTop: '#c07a34',
  ground:    '#8a5220',
  groundDim: '#5c3414',
  groundDark:'#38200c',
  abyss:     '#0b0a12',
  haze:      '#7fa8d8',
  hazeDark:  '#6b93c4',

  rust:      '#b0642c',
  rustLite:  '#dc9048',
  rustHi:    '#f0bc78',
  rustDark:  '#733c14',
  rustDeep:  '#48230c',
  metal:     '#8c8c98',

  skin:      '#f4b888',
  skinDark:  '#c07c50',
  hair:      '#2c1c14',
  pants:     '#3c4a6c',
  shoe:      '#d02c1c',   // красные конверсы
  pack:      '#c8281e',   // красный рюкзак
  llama:     '#efe4cf',
  llamaDark: '#c0ae92',

  ink:       '#1a1410',
  paper:     '#fff2dc',
};

// рубахи толпы — каждый человечек свой, но палитра общая
export const SHIRTS = ['#3fa64c', '#d8a020', '#8c4ccc', '#2c8cc4', '#d85c3c', '#607890'];

// и масти: в давке видно в основном макушки, одинаковые слиплись бы в кашу
export const HAIRS = ['#2c1c14', '#6b3a1c', '#d8b45c', '#8c8c98', '#1a1620', '#b4542c'];
